import { TaglineReveal } from "@/components/landing/tagline-reveal";

export function TaglineSection() {
  return (
    <section className="py-24 sm:py-32 px-4">
      <TaglineReveal text="Every invoice you send is a small proof of how your business runs. Statsix Invoice makes sure it looks like it." />
    </section>
  );
}
